# Core FastAPI framework with all standard features
fastapi[standard]>=0.104.1

# ASGI server for production deployment
uvicorn[standard]>=0.24.0

# File upload and form data handling
python-multipart>=0.0.6

# Configuration management
pydantic-settings>=2.0.3
python-dotenv>=1.0.0

# OpenAI API client (direct integration)
openai>=1.51.0

# Token counting for OpenAI models
tiktoken>=0.5.0

# Vector database (direct ChromaDB without LangChain wrappers)
chromadb>=0.5.11

# PDF text extraction
pypdf>=4.2.0

# HTTP client for API calls (if needed for external services)
httpx>=0.25.0

# JSON Web Token support (for future authentication)
# pyjwt>=2.8.0

# Database drivers (for future persistence)
# psycopg2-binary>=2.9.7  # PostgreSQL
# sqlite3 is built into Python

# Development and testing utilities
# pytest>=7.4.3
# pytest-asyncio>=0.21.1