# SmartDocs AI Backend - Production Environment Example
# Copy to .env and fill required secrets. Do NOT commit real secrets.

# === Core Application ===
ENVIRONMENT=production
DEBUG=false
HOST=0.0.0.0
PORT=8000
RELOAD=false

# === OpenAI / LLM ===
OPENAI_API_KEY=replace_me
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.1

# === Vector Store ===
VECTOR_STORE_PROVIDER=chroma        # chroma | pinecone
VECTOR_STORE_PERSIST_DIR=backend/vectorstores

# === Document Processing ===
CHUNK_SIZE=1000
CHUNK_OVERLAP=150
RETRIEVAL_K=4

# === Upload Limits ===
MAX_UPLOAD_SIZE_MB=50
ALLOWED_FILE_TYPES=application/pdf

# === CORS ===
CORS_ORIGINS=https://lakkakula-saidev.github.io,https://lakkakula-saidev.github.io/smartdocs-ai
CORS_ALLOW_CREDENTIALS=true

# === Logging ===
LOG_LEVEL=INFO
LOG_FORMAT=structured

# === Pinecone (only if VECTOR_STORE_PROVIDER=pinecone) ===
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=smartdocs-ai

# === Security / Future Auth (placeholders) ===
# API_KEY_AUTH_SECRET= # for custom header auth
# JWT_SECRET= # for JWT-based auth

# === Performance Tuning ===
# WORKER_PROCESSES=2           # used by process manager if adopted
# REQUEST_TIMEOUT_SECONDS=60

# === Observability (optional integrations) ===
# SENTRY_DSN=
# OTEL_EXPORTER_OTLP_ENDPOINT=

# === Frontend Coordination ===
# Set frontend VITE_API_URL to your deployed backend base URL (no trailing slash)
# Example: VITE_API_URL=https://smartdocs-backend.onrailway.app

# End of file