# Minimal Docker ignore (post-cleanup)

# Python env & caches
.venv/
__pycache__/
*.py[cod]

# VCS
.git/
.github/

# Tests & coverage (removed from repo)
test_*.py
*_test.py
.pytest_cache/
coverage.xml
.coverage
htmlcov/

# Local env / secrets
.env
.env.*
!.env.example

# Build artifacts
dist/
build/

# Editors / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Logs / temp
*.log
tmp/
temp/
*.tmp

# Vector store runtime data (mount as volume instead)
# Ignore all known historical and current locations plus any nested occurrences
vectorstores/
backend/vectorstores/
backend/backend/vectorstores/
**/vectorstores/

# Frontend (not needed for backend image build if building separately)
frontend/

# Node modules (if root context ever used)
node_modules/

# Archives
*.zip
*.tar.gz
*.rar
*.7z

# Misc
*.bak
*.backup